<%= simple_form_for [@book, @booking] do |f| %>
  <%= f.input :start_date, placeholder: 'Start Date', label: false, input_html: { class: 'devise-form-input' } %>
  <%= f.input :end_date, label: false, placeholder: 'End Date', input_html: { class: 'devise-form-input' } %>
  <%= f.association :book, label: false, input_html: { class: 'devise-form-input' } %>
  <%= f.button :submit, "Book now", class: 'btn-violet-solid mt-4 mx-auto' %>
<% end %>
